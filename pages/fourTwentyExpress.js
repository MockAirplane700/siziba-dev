import { View, Text, SafeAreaView } from "react-native";

const FourTwentyExpress = ({navigation}) => {
    return (
        <SafeAreaView style={{flex: 1}}>

        </SafeAreaView>
    );
}//end login page

export default FourTwentyExpress;